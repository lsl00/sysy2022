float sin(float x){
    1.0;
    float res = 0;
    float sign = 1.0;
    float fac = 1.0,poly = x;
    int i = 1;
    while(i <= 30){
        res = res + sign * ploy / fac;
        sign = sign * (-1.0);
        ploy = ploy * x * x;
        fac = fac / (i+1) / (i+2);
        i = i + 2;
    }
    return res;
}
int main(){
    putint(sin(1.0));
}